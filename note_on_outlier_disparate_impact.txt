 ---                                                                                                                                     >
  3   Feature 1: Row-Level Reconstruction Scores                                                                                              >
  4                                                                                                                                           >
  5   What needs to change                                                                                                                    >
  6                                                                                                                                           >
  7   scoring.py — two new functions returning per-row results:                                                                               >
  8
  9   - calculate_row_level_scores_categorical(original, reconstructed, hidden_features) → DataFrame(n_rows × n_features) of binary 0/1 correc>
 10    per cell. The rarity-weighting still makes sense here as a per-row scalar: row_score_i = Σ_j correct_ij × rarity(val_ij) / Σ_j
 11   rarity(val_ij). This naturally down-weights rows that only have common-value features and up-weights rows where rare values were correct>
 12   recovered.
 13   - calculate_row_level_scores_continuous(original, reconstructed, hidden_features) → DataFrame(n_rows × n_features) of normalized absolute
 14   error per cell (0 = perfect, 1 = worst), plus mean row score.
 15
 16   scoring.py — outlier labeling function:
 17
 18   label_outliers(df, qi_cols, cat_cols, num_cols, method='isolation_forest', percentile=90) → returns (scores_series, is_outlier_bool_seri>
 19   Two methods to support:
 20   - IsolationForest (sklearn) — most widely used, practical, interpretable anomaly score
 21   - Gower k-NN distance — already implemented in mia.py; distance to k-th nearest neighbor in training set; directly interpretable in Knor>
 22   Ng (1998) distance-based outlier framework
 23
 24   For the which features to define outliers on question: I'd suggest computing outlier score in QI space only (public features), since:
 25   1. That's what the attacker observes — records that are unique in QI space are most at-risk
 26   2. It's the most privacy-relevant framing (uniqueness in public attribute space)
 27   3. It aligns with k-anonymity intuitions from the literature
 28
 29   master_experiment_script.py — new config section:
 30
 31   row_level_analysis:
 32     enabled: true
 33     output_dir: "/home/golobs/data/row_scores/"  # null = same dir as data
 34     compute_outliers: true
 35     outlier_method: "isolation_forest"  # or "gower_knn"
 36     outlier_percentile: 90
 37
 38   When enabled, saves a CSV per run: {output_dir}/{run_id}_row_scores.csv with columns [qi_cols..., hidden_feat_cols (true),
 39   hidden_feat_cols_recon, RA_row_{feat}..., RA_row_mean, outlier_score, is_outlier].
 40
 41   ---
