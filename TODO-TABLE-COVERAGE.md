# TODO: regenerate every paper table and figure from committed data

**Why it matters.** The PoPETs "Reproduced" badge asks for a clear mapping from
claims to experiments to results, with minimal manual effort. We meet that for
the five main claims -- every table carrying one regenerates exactly from
`results.db` via `python reproduce.py`. The remaining objects are supporting
material, and for most of them the *data* is already committed; only the
generator is missing. Finishing this turns "the numbers are in a database" into
"every number in the paper rebuilds with one command."

**Target.** `python reproduce.py` emits a file for every table and figure in the
paper, each byte-comparable against `expected_output/tables/`.

**How to close one item.** Write the generator in
`experiment_scripts/regen_camera_tables.py`, then in
`experiment_scripts/paper_objects.py` set that object's `generator`, `output`,
`check` and `status="done"`. `reproduce.py`, `test.sh` and the tables below all
follow automatically -- run `python reproduce.py --docs` to refresh this file.

**Check as you go:** each new table must match the printed one. Where it does
not, say so in the generator's docstring and in the manifest row's `note` --
`table7()` and `table9()` set that precedent, and it is how the stale MIA cells
were caught.

<!-- BEGIN generated: coverage -->
<!-- Generated by 'python reproduce.py --docs'. Do not edit by hand. -->

**Status:** 12 of the manuscript's 33 labelled tables and figures regenerate
today. This file tracks the other 21: 12 that still need work, and 9
that are not derived from this repository at all.

Check the live figure at any time with `python reproduce.py --list`.

---

## Done -- regenerates from committed data (12 labelled + 2 unlabelled)

| Paper object | Output | Source | Verified by `test.sh` |
|---|---|---|---|
| `tab:ra_mean_adult` | `table1_ra_mean_adult.tex` | `results.db` | yes |
| `tab:quality_overview` | `table2_quality_overview.tex` | `quality_results_merged.csv` | yes |
| `tab:eps_sweep` | `table4_eps_sweep_compact.tex` | `results.db` | yes |
| `tab:eps_full` | `table4_eps_sweep_full.tex` | `results.db` | yes |
| `tab:eps_stats` | `STATS_eps_curve.md` | `results.db` | yes |
| `tab:mia_comparison` | `table6_mia_comparison.tex` | `mia_comparison_results.csv` | yes |
| `tab:memorization_and_ds_risk` | `table7_memorization.tex` | `linear_sweep_adult_1000_20260822_182357.csv + ds_risk_scores.csv` | yes |
| _(no label)_ | `STATS_memorization.md` | `linear_sweep_adult_1000_20260822_182357.csv + ds_risk_scores.csv` | yes |
| `tab:disparate_impact` | `table9_disparate_impact.tex` | `per_attack_disparity_postrepair.csv` | yes |
| `fig:eps_curves` | `fig_eps_curves.pdf` | `results.db` | no -- binary |
| _(no label)_ | `fig_eps_curves_perattack.pdf` | `results.db` | no -- binary |
| `tab:ra_mean_cdc` | `table_ra_mean_cdc.tex` | `results.db (cdc_diabetes, 1k)` | yes |
| `tab:cdc_100k` | `table_cdc_100k.tex` | `results.db (cdc_diabetes, 100k)` | yes |
| `tab:ra_mean_nist_sbo` | `table_ra_mean_nist_sbo.tex` | `results.db (nist_sbo, 1k)` | yes |

## Tier A -- data already committed, generator missing (8)

Each of these needs only a new function in `regen_camera_tables.py` and a
`status="done"` flip in the manifest. No experiments to re-run, no files to
add. This is the bulk of the remaining work and should be done first.

| Paper object | Data is in | Notes |
|---|---|---|
| `tab:feature_eps_breakdown` | `results.db (feature_scores)` | wandb_to_latex_epsilon_sweep.py already builds this from the DB -- fold that logic in rather than rewriting it. |
| `tab:memorization_california` | `results.db (california, train/nontraining)` | Continuous dataset: the metric is NRMSE, not R_adv, so it needs its own formatter rather than reusing table7(). |
| `tab:quality_arizona` | `quality_results_merged.csv` | One parameterised generator covers all four per-dataset quality tables; synth_quality_to_latex.py has the column logic. |
| `tab:quality_cdc` | `quality_results_merged.csv` | See tab:quality_arizona. |
| `tab:quality_sbo` | `quality_results_merged.csv` | See tab:quality_arizona. |
| `tab:quality_california` | `quality_results_merged.csv` | See tab:quality_arizona. |
| `tab:disparity_perfeature` | `per_attack_disparity_postrepair_perfeat.csv` | CSV is committed. |
| `tab:disparity_perattack` | `per_attack_disparity_postrepair.csv` | Ratio of outlier to non-outlier row-level R_adv; the CSV has both columns. |

## Tier B -- data committed, formatter is WandB-only (3)

The data is in `results.db`, but the only existing formatter needs our
Weights & Biases credentials, so a reviewer cannot run it. Port the
formatting logic to read the DB, the way `wandb_to_latex.py` already does.

| Paper object | Data is in | Notes |
|---|---|---|
| `tab:qi_analysis_adult` | `results.db` | qi_analysis/wandb_to_latex_qi.py needs our Weights & Biases credentials, so a reviewer cannot run it. The DB has every QI variant; port the formatter the way wandb_to_latex.py already reads the DB. |
| `tab:qi_analysis_cdcdiabetes` | `results.db` | See tab:qi_analysis_adult. |
| `tab:linear_sweep_summary` | `results.db (914 LinearReconstruction runs)` | linear_sweep_to_latex.py is WandB-only. Port it to read the DB. |

## Tier C -- needs a result file that is not committed yet (1)

| Paper object | Data is in | Notes |
|---|---|---|
| `fig:heatmap_ensemble` | `ensembling_heatmap_results_*.csv` | 17 timestamped candidate CSVs exist and none is marked authoritative. Decide which run produced the printed figure, commit that one, then wire plot_ensembling_heatmap.py in. Files are <=300 KB; the work is the provenance decision, not the plumbing. |

## Tier D -- not derived from this repository (9)

These are correctly out of scope. They are listed so the count reconciles
and nobody goes looking for a generator.

| Paper object | Why |
|---|---|
| `tab:nist_results` | Transcribed from NIST's official results, not computed here. |
| `tab:datasets` | Hand-written prose description of the five datasets. |
| `tab:qi_def_adult` | Hand-written QI membership matrix -- documentation, not a result. Worth cross-checking against the QIs dict in get_data.py. |
| `tab:qi_def_cdc` | Hand-written QI membership matrix. See tab:qi_def_adult. |
| `tab:qi_def_arizona` | Hand-written QI membership matrix. See tab:qi_def_adult. |
| `tab:qi_def_california` | Hand-written QI membership matrix. See tab:qi_def_adult. |
| `tab:qi_def_sbo` | Hand-written QI membership matrix. See tab:qi_def_adult. |
| `fig:threat_model` | Hand-drawn diagram, not generated from data. |
| `fig:taxonomy` | Hand-drawn diagram, not generated from data. |

<!-- END generated -->

---

## Known discrepancies between the regenerated tables and the printed paper

The database was repaired in August 2026 (the float-binned encoding bug), and
some printed tables still carry pre-repair numbers. The artifact regenerates from
the repaired database, so those cells differ. Every difference found so far is
listed here. **None of them changes a claim in the paper**, but a reviewer
diffing an output against the PDF will see them, so they are stated up front
rather than discovered.

### `tab:ra_mean_adult` (Table 1) — 128 of 252 cells

The largest divergence, and the one to decide about first.

| What differs | Cells | Why |
|---|---|---|
| The five MST columns, nearly every row | 81 | MST synth was regenerated in May 2026; the printed columns predate it. Moves are large (KNN at $\varepsilon{=}10$: 13.2 → 21.7). |
| `CondDDPM` and `CondRePaint` rows, entirely | 16 | Those runs were flagged `confidence='broken'` and excluded; there are no post-repair replacements, so the generator prints `---`. This is Fault 1 in `manuscript/camera_ready/CAMERA_READY_PLAN.md`, still open. |
| `CoBP-RA`, `CondMST`, `MultiHeadMLP` rows, non-MST columns | 23 | Post-repair re-runs. Largest move 2.8 pp (CoBP-RA on CTGAN). |
| The `Avg.` row | 8 | Follows from the above. |

**Every other row — KNN, Naive Bayes, Logistic Regression, SVM, Random Forest,
LightGBM, MLP, TabPFN, ARFFormer, Mode, Random, Copy — matches the printed table
exactly outside the MST columns.**

The generator also does not emit the printed `Best ensemble` and `Best chain`
rows; those come from the chaining/ensembling sweeps rather than the attack grid.

### `tab:ra_mean_nist_sbo` — the MST columns

MST $\varepsilon{=}0.1$ and $\varepsilon{=}1$ were superseded and never re-run,
so they print `---`. The other three MST budgets survive on 2 disjoint samples
rather than 5 and move by up to 0.4 pp. The eight non-MST columns are exact.
Closing this needs new runs, not a new generator.

### `tab:ra_mean_cdc` — two columns

MST $\varepsilon{=}0.1$ and AIM $\varepsilon{=}3$ were repaired and re-run:
9 of 75 cells, largest move 0.9 pp. Everything else is exact.

### `tab:mia_comparison` (Table 6) — seven cells

The printed table mixes pre- and post-repair runs: the NIST Arizona block is
current, but parts of the Adult and CDC blocks come from the May 2026 runs.
Seven cells differ, all by <=0.01 AUC.
`expected_output/tables/table6_mia_comparison.tex` is the post-repair version.
Bolding shifts on the Adult MST columns, because SynthDistance now edges NNDR.

### A naming note

The row printed as `CoBP-RA$^\dagger$` is **not the same attack variant in every
table**. In `tab:ra_mean_cdc`, `tab:cdc_100k` and `tab:ra_mean_nist_sbo` it is
`MarginalRF_graphQI_entropyBP` (QIGraph + EntropyBP) -- verified by matching
every non-superseded cell of all three exactly, which no other variant does. In
Table 1 it is the plain attack. That is worth either making consistent or
stating in a caption.

### The decision this needs

Either paste the regenerated tables into the camera-ready, so the paper and the
artifact agree, or leave the paper as printed and let the artifact document the
divergence (which is what it now does). The first is cleaner for the
"Reproduced" badge. Both are defensible; it is an author call, and it is the
last substantive item before submission.

## Suggested order

1. **`tab:ra_mean_cdc`, `tab:cdc_100k`, `tab:ra_mean_nist_sbo`** -- one
   parameterised generator covers three tables and is the template for the rest.
2. **The four `tab:quality_*` tables** -- again one generator.
3. **`tab:feature_eps_breakdown`, `tab:memorization_california`,
   `tab:disparity_perfeature`, `tab:disparity_perattack`** -- one each.
4. **Tier B** -- port the two formatters off Weights & Biases.
5. **Tier C** -- resolve the ensembling-heatmap provenance, then wire it up.

Nothing in this list requires re-running an experiment.
